{
  "lex": 1,
  "id": "app.skydrops.bats.card",
  "defs": {
    "main": {
      "type": "record",
      "description": "A single card cast into the sky for B.A.T.S.",
      "record": {
        "type": "object",
        "required": ["text", "gameID", "createdAt"],
        "properties": {
          "text": { "type": "string", "maxLength": 1000 },
          "gameID": { "type": "string", "maxLength": 12 },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
